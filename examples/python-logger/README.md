# Python logger example

This example reads a flat Signal Contract v1 object from disk and prints one normalized awareness line.

```bash
python3 examples/python-logger/main.py examples/python-logger/sample_signal.json
```
